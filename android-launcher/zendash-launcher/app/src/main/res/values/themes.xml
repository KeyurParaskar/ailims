<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Window-level theme — sets the window background to pure black so
        there's no white flash during cold start on AMOLED screens.
        All actual theming is handled by Compose's ZenDashTheme.
    -->
    <style name="Theme.ZenDash" parent="android:Theme.Material.NoTitleBar.Fullscreen">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
